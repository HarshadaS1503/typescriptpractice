const car: { type: string, model: string, year: number } = {
    type: "Toyota",
    model: "Corolla",
    year: 2009
  };
  
  console.log(car);

const citylist: {name:string, code?: number}=
{
    name:"mumbai",
    
}

console.log(citylist);